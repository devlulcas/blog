---
title: Hello Rust 🦀
thumbnail: /pictures/a_r_crab.svg
alt: ferris o carangueijo mascote do rust
tags:
  - rust
  - dev
  - linguagens
date: 10/06/2022
excerpt: Os meus primeiros passos no mundo Rust
---

# 🦀 APRENDENDO RUST 🦀

Em 2022 eu decidi explorar mais algumas linguagens fora da minha zona de conforto (web) e uma das mais interessantes e comentadas que eu vi foi a linguagem de programação Rust.

Rust é uma linguagem compilada, estáticamente e fortemente tipada, sem `garbage colector` e com uma maneira diferente de programar.

A falta do `garbage colector` faz com que seja necessário que nós (programadores) tenhamos que gerenciar a memória manualmente. Em linguagens como C essa responsabilidade é realmente entregue nas mãos dos programadores por completo, o que pode levar a falhas complexas de resolver. Em Rust essas falhas são previnidas com o sistema de `Ownership`. Graças a isso Rust consegue "facilitar" o gerenciamento de memória sem a perca de desempenho que o `garbage colector` traz.

> `garbage colector\coletor de lixo` - Um processo de automação de gerenciamento de memória que tenta liberar memória que foi alocada pelo programa, mas que não está mais sendo usada.

<br />

> `Ownership\propriedade` - Basicamente um conjunto de regras verificadas em tempo de compilação que garantem a segurança e bom uso da memória. Ser verificado em tempo de compilação quer dizer que isso não vai afetar a performance de execução do seu programa, ele simplesmente não vai compilar se você quebrar as regras.

## INSTALANDO TUDO

Foi extremamente fácil instalar o Rust. As informações são fáceis de achar e tudo correu normalmente. Fiz minha instalação via o `rustup`, uma ferramenta de linha de comando que cuida das versões da linguagem e de outras ferramentas.

```bash
$ curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh
```

Em relação a IDE/Editor de texto eu simplesmente usei o VSCode mesmo, só precisei instalar uma extensão e deu tudo certo.

```bash
code --install-extension rust-lang.rust-analyzer
```

Em seguida testei se tinha dado tudo certo com o clássico "Hello World!"

```rs
// hello.rs
fn main() {
    println!("Hello World!");
}
```

Para compilar basta digitar `rustc hello.rs`

## POR ONDE ESTOU APRENDENDO ?

> Basicamente pelo [livro da linguagem Rust](https://doc.rust-lang.org/book/ch01-01-installation.html) e pelo
